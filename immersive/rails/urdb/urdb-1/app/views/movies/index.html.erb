<h1>All movies</h1>

<div class="row">
	<% @movies.each do |movie| %>
	  <div class="large-4 columns">
	  	<h3><%= movie.title %></h3>
			<p><%= image_tag movie.image %></p>
	  </div>
	<% end %>
</div>

	
